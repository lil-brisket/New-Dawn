# @dawn/types

Shared TypeScript interfaces for Dawn. Definitions (static) are typed here; instances live in `player/` and `battle/`.

## Extension points

- Add new `SkillEffect` variants in `skill/effects.ts`
- Add new `BattleCommand` variants in `commands/battle.ts`
- Add new `BattleEvent` variants in `events/battle.ts`
